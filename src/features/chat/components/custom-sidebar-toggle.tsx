import { SidebarTrigger } from '@/components/ui/sidebar';

export default function CustomSidebarToggle() {
  // TODO: will change later to stream
  // const { openMobileNav } = useChatContext('ChannelHeader');
  return <SidebarTrigger className='-ml-1' />;
}
